export enum QUERY_SCOPES {
  issues = 'issues',
  labels = 'labels',
  users = 'users'
}

export enum QUERY_ENTITIES {
  issue = 'issue',
  comments = 'comments',
  user = 'user'
}

export enum ISSUE_STATUS {
  backlog = 'Backlog',
  todo = 'Todo',
  inProgress = 'In Progress',
  done = 'Done',
  cancelled = 'Cancelled'
}
